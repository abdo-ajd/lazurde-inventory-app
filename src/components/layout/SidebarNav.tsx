// src/components/layout/SidebarNav.tsx
"use client";

// This component is no longer used directly in DashboardLayout 
// as navigation has moved to Header or is page-specific.
// Keeping the file to prevent potential import errors if referenced elsewhere,
// but it will not render anything significant.

export default function SidebarNav() {
  return null; 
}
